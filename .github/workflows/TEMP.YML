- name: Debug PAT
  run: |
    if [ -z "${{ secrets.PAT_TOKEN }}" ]; then
      echo "PAT_TOKEN is EMPTY!"
      exit 1
    else
      echo "PAT_TOKEN exists."
      echo "Length: ${#PAT}"
    fi
  env:
    PAT: ${{ secrets.PAT_TOKEN }}
